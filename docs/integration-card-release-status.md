# 综合运用卡片发布状态

本次冻结的综合运用要求为 1,301 对，运行时已有代表卡覆盖 107 对。当前冻结集合分为：

| 状态 | 数量 |
| --- | ---: |
| 已审核（运行时已有） | 107 |
| 已合并新增 | 0 |
| 已接入新增 | 0 |
| 已发布新增 | 0 |
| 暂缓 | 0 |
| 开放未决 | 1,194 |

工作目录中的 166 张候选稿仍是草稿／审核工作证据，没有作者 receipt、完整独立审核法定人数和正式文件 hash，因此不计入已审核、已合并或网站覆盖。严格 receipt 审计须在正式合并文件产生后运行；当前非严格集合审计已确认要求、已审核、暂缓和开放未决集合互不重叠，且并集等于 1,301 对。

验证结果：`npm test` 928/928 通过，`npm run audit:usage`、`npm run audit:eligibility`、类型检查、lint 和 build 通过。Pages 发布保留到开放未决项完成、审核 receipt 固定并接入运行时之后；CI 已在构建前运行 `npm run audit:integration`。
