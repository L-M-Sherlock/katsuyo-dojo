# 综合运用分批发布记录

2026-09-21 本次发布只接入已完整合并、具有独立审核收据的 64 个协议 3 批次。

| 集合 | 数量 |
| --- | ---: |
| 冻结词义／形式要求 | 1,301 |
| 原有综合运用代表卡 | 107 |
| 本次新增正式卡 | 949 |
| 本次运行时综合运用卡 | 1,056 |
| 尚未发布的开放配对 | 245 |
| 暂缓常规出题配对 | 0 |
| 网站全部用法卡 | 17,361 |

原有 16,412 张卡逐对象保持不变。本次没有调整课程适用性，也没有把未完成内容改称暂缓。缺少卡片的综合运用题仍按原有规则出题，仅在有精确匹配卡时显示场景。

发布范围由 [v2 集合账本](integration-stage-requirements.v2.json) 的 `approved` 明确列出，`open` 保留全部 245 对尚未发布的要求。两者互斥，合起来恰好等于原始 v1 的 1,301 对；v1 文件原始字节保持不变。

[压缩审核证明](integration-release-proof.json.gz) 保存作者快照、两名独立审核者报告、法定人数汇总和正式合并稿。证明可脱离临时任务目录核验。严格覆盖审计把运行时的每个综合运用卡对象与证明逐一对照；缺卡、多卡、内容漂移、无效收据或与开放集合重叠都会失败。部分发布不会放宽这些验证。

续跑期间主动撤回的作者稿和审核报告已保留在任务历史中，未纳入本次发布证明。需要复审的五个阶段及一份模板化扩写稿仍在暂停的 `work/integration-macos-20260921` 中；继续任务时必须先处理这些记录，不能把工作目录的原稿直接接入运行时。

发布验收采用 `docs/publishing.md` 的全部审计、测试、学习模拟、类型检查、lint 和构建，并由同一次 GitHub Actions 运行生成及部署 Pages artifact。
