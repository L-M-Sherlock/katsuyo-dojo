# 综合运用完整发布记录

2026-09-21 本次发布已完成 82 个协议 3 批次，所有 1,301 个冻结配对均通过独立审核并接入。

下表为综合运用完整发布时的历史快照。后续动作卡修订三张、撤回死ぬ的一张后，当前全站为 17,605 张，综合运用仍为 1,301 对。v1/v2/v3 账本及压缩证明保留原始字节；后续变更单独记录在[动作审核账本](usage-card-actions-review.json)，测试还原这些明确差异后仍核对原发布哈希。

| 集合 | 数量 |
| --- | ---: |
| 冻结词义／形式要求 | 1,301 |
| 原有综合运用代表卡 | 107 |
| 本轮累计新增正式卡 | 1,194 |
| 本次补齐新增卡 | 245 |
| 本次运行时综合运用卡 | 1,301 |
| 尚未发布的开放配对 | 0 |
| 暂缓常规出题配对 | 0 |
| 网站全部用法卡 | 17,606 |

原始 16,412 张卡、上一分批版本的全部 17,361 张卡均逐对象保持不变。本次补齐上一版本开放的 245 对，没有调整课程适用性或暂缓任何配对；卡片仍只匹配相同词义和完整目标形式。

发布范围由 [v3 集合账本](integration-stage-requirements.v3.json) 的 `approved` 明确列出，`open` 为空；恰好覆盖原始 v1 的 1,301 对，v1 文件原始字节保持不变。

[v2 分批账本](integration-stage-requirements.v2.json) 和[对应证明](integration-release-proof.json.gz) 保留为历史发布快照。新增测试验证 v2 文件字节未变、旧卡对象不变，以及本次新增集合恰好等于 v2 的开放集合。

[压缩审核证明](integration-release-proof.v3.json.gz) 保存作者快照、两名独立审核者报告、法定人数汇总和正式合并稿。证明可脱离临时任务目录核验。严格覆盖审计把运行时的每个综合运用卡对象与证明逐一对照；缺卡、多卡、内容漂移、无效收据或与开放集合重叠都会失败。

续跑期间主动撤回的作者稿和审核报告已保留在任务历史中，未纳入本次发布证明。它们不改变最终已审核卡片；任务目录中的历史失败稿不能直接接入运行时。

发布验收采用 `docs/publishing.md` 的全部审计、测试、学习模拟、类型检查、lint 和构建，并由同一次 GitHub Actions 运行生成及部署 Pages artifact。
